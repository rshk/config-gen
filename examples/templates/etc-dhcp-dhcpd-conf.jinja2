################################################################################
## /etc/dhcp/dhcpd.conf
## Autogenerated by config-gen
################################################################################

# The ddns-updates-style parameter controls whether or not the server will
# attempt to do a DNS update when a lease is confirmed. We default to the
# behavior of the version 2 packages ('none', since DHCP v2 didn't
# have support for DDNS.)
ddns-update-style none;

# option definitions common to all supported networks...
option domain-name "{{ settings.dhcp.domain_name }}";
option domain-name-servers {{ settings.dhcp.dns_servers }};

default-lease-time 600;
max-lease-time 7200;

# If this DHCP server is the official DHCP server for the local
# network, the authoritative directive should be uncommented.
#authoritative;

# Use this to send dhcp log messages to a different log file (you also
# have to hack syslog.conf to complete the redirection).
log-facility local7;


##========================================
## Subnet/pools configuration
##========================================

subnet {{ settings.dhcp.subnet }} netmask {{ settings.dhcp.subnet_mask }} {
  option routers {{ settings.dhcp.routers }};

  # Unknown clients get this pool.
  pool {
    max-lease-time 300;
    range {{ settings.dhcp.pub_pool_range }};
    allow unknown-clients;
  }

  # Known clients get this pool.
  pool {
    max-lease-time 28800;
    range {{ settings.dhcp.priv_pool_range }};
    deny unknown-clients;
  }
}


##========================================
##  Fixed address hosts
##========================================

{% for host in hosts %}
host {{ host.0 }} {
    hardware ethernet {{ host.1 }};
    fixed-address {{ host.2 }};
}
{% endfor %}
